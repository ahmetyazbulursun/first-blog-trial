@using BlogDeneme.Models.Entity
@using PagedList
@using PagedList.Mvc
@model PagedList.IPagedList<Tbl_Admin>

@{
    ViewBag.Title = "Profilim";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="clearfix">
    <div class="float-left">
        @Html.PagedListPager((IPagedList)Model, sayfa => Url.Action("KategoriIslemleri", new { sayfa }), new PagedListRenderOptions
        {
            LiElementClasses = new List<string>
            { "page-link"}
        })
    </div>

    <div class="float-right">
        <a href="/Vitrin/AdminEkle" class="btn btn-info">Yeni Admin Ekle</a>
    </div>
</div>


<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover">
        <tr align="center">
            <th>Kullanıcı Adı</th>
            <th>Parola</th>
            <th>Sil</th>
            <th>Güncelle</th>
        </tr>
        @foreach (var x in Model)
        {
            <tr align="center">
                <td>@x.KULLANICIADI</td>
                <td>@x.PAROLA</td>
                <td>@Html.ActionLink("Sil", "AdminSil", new { id = x.ID }, new { @class = "btn btn-outline-danger", onclick = "return confirm('Kategoriyi Silmek İstediğinize Emin Misiniz?')" })</td>
                <td><a href="/Vitrin/AdminGetir/@x.ID" class="btn btn-outline-success">Güncelle</a></td>
            </tr>
        }
    </table>
</div>